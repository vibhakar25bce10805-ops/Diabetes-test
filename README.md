# Diabetes-test
This project predicts diabetes using a Random Forest model trained on the Pima Indians dataset. It includes data preprocessing, feature scaling, model evaluation (accuracy, ROC-AUC), and visualizations. A function is provided to predict diabetes for new patients based on medical inputs.
